####################################### Services
services:
  node-exporter:
    image: quay.io/prometheus/node-exporter
    container_name: core-node-exporter-c
    command:
      - "--path.rootfs=/host"
    pid: host
    volumes:
      - "/:/host,rslave"
    networks:
      - network-core
    restart: always
    # ports:
    #   - 9100:9100

####################################### Networks
networks:
  network-core:
    external: true
